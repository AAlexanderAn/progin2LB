{
    "name": "proginLB2",
    "version": "1.0.0",
    "scripts": {
        "test": "echo 'Running tests...' && exit 0",
        "build": "echo 'Building...' && mkdir -p dist && echo 'Hello World' > dist/index.html"
    }
}